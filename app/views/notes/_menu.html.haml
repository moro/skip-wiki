.by-date.menu-item
  %h3&= _("Updated")
  %ul
    - group_by_date(note.pages.recent(10)).each do |date, ps|
      %li
        %h4&= date
        %ul.submenu
          - ps.each do |p|
            %li= link_to(h(p.display_name), note_page_path(note, p))
